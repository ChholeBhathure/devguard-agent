# 🛡️ DevGuard

![DevGuard CI Pipeline](https://github.com/ChholeBhathure/devguard-agent/actions/workflows/ci.yml/badge.svg)
![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)
![Python Version](https://img.shields.io/badge/python-3.10%7C3.11%7C3.12-blue)

**DevGuard** is a production-grade, local-first autonomous Python security auditor and code reviewer powered by Gemini 2.5-Flash. It analyzes local code bases via AST (Abstract Syntax Tree) representation to detect potential vulnerabilities, bugs, and performance bottlenecks without sending raw source code files externally.

---

## 🚀 Features

* **Local-First Security Audit**: Parses local Python AST structures before querying LLM providers.
* **Multi-Format Export**: Generates clean Markdown and HTML security reports directly via CLI flags.
* **Rich Console TUI**: Uses `rich` for structured terminal outputs and formatted markdown rendering.
* **Automated CI/CD**: Matrix testing verified across Python 3.10, 3.11, and 3.12 using GitHub Actions.
* **Docker Ready**: Fully containerized environment for isolated executions.

---

## 📄 Sample Audit Output

Check out an example audit report generated by DevGuard:
* [View Sample Markdown Report](examples/sample-audit-report.md)
* [View Sample HTML Report](examples/sample-audit-report.html)

---

## 🏗️ Architecture & Project Structure

DevGuard follows a modular, local-first design pattern separating retrieval, analysis, and execution:

devguard-agent/
├── src/devguard/
│   ├── cli.py         # Entry point, CLI argument parsing, report formatting
│   ├── retriever.py   # Directory scanning, AST traversal, filtering via config
│   ├── agent.py       # Gemini 2.5-Flash API orchestration & prompt logic
│   └── config.py      # .devguardrc.json reader & default fallbacks
├── tests/             # Pytest test suite validating CLI & core modules
├── Dockerfile         # Container image definition for isolated execution
├── pyproject.toml     # Package configuration & dependencies
└── .devguardrc.json   # Directory exclusion rules

**Workflow:**
1. **`retriever.py`** scans the target directory, parses Python files into AST nodes, and builds a file structure map while respecting ignore patterns.
2. **`agent.py`** constructs structured security prompts containing code context and transmits them to the **Gemini 2.5-Flash** model.
3. **`cli.py`** receives the security analysis, formats it into clean Markdown/HTML using `markdown`, and saves the final audit report locally.

---

## 🏗️ Architecture Diagram

```mermaid
graph TD
    A[Target Directory] -->|File Traversal| B[retriever.py: AST Parser]
    B -->|Filtered AST & Metadata| C[agent.py: Gemini 2.5-Flash]
    C -->|Security Vulnerabilities & Bug Report| D[cli.py / Web API]
    D -->|Render Output| E[HTML / Markdown Report]

---

## 🛠️ Tech Stack

* **Language:** Python 3.12
* **LLM Engine:** Gemini 2.5-Flash
* **CLI & Formatting:** `rich`, `markdown`
* **Testing:** `pytest`
* **Containerization:** Docker

---

## 🚀 Quickstart

### Prerequisites
* [Docker Desktop](https://www.docker.com/products/docker-desktop/) (for containerized execution)
* Python 3.10+ (for local development)
* A Gemini API key from [Google AI Studio](https://aistudio.google.com/)

---

### 1. Set Up Environment Variables

Create a `.env` file in the root directory:

```env
GEMINI_API_KEY=your_gemini_api_key_here

